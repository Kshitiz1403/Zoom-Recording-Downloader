{
  "name": "zoom-recording-manager",
  "version": "1.0.0",
  "description": "App for downloading the cloud recordings on Zoom using Oauth2 Client ID and Secret to create an OAuth2 token used to call the Zoom API.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "author": "Kshitiz Agrawal",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^3.0.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "localtunnel": "^2.0.2",
    "open": "^8.4.0",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
